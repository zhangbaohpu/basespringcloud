spring:
  application:
    name: configclient
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8762 #本地config server访问路径
      fail-fast: true #设置客户端优先判断config server是否获取正常