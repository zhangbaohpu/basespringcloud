server:
  port: 8030
spring:
  application:
    name: gateway
eureka:
  client:
    service-url:
      defalultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
zuul:
  routes:
    provider: /p/** #给服务提供者provider提供服务映射